# Microsoft SQL Server Connection Configuration
# Copy this file to .env and update with your actual database credentials
# 
# IMPORTANT: 
# - Never commit the actual .env file to version control
# - Use strong passwords and secure connections
# - Test in development mode first

# Required: SQL Server hostname or IP address
MSSQL_SERVER=your-server.database.windows.net

# Required: Database name to connect to
MSSQL_DATABASE=YourDatabase

# Required: Username for SQL Server authentication
MSSQL_USER=your_username

# Required: Password for SQL Server authentication
# Use a strong password and consider using managed identities in production
MSSQL_PASSWORD=your_secure_password_here

# Optional: SQL Server port (defaults to 1433 if not specified)
MSSQL_PORT=1433

# Development vs Production Mode
# "true"  = Development: Allows self-signed TLS certificates, detailed error messages
# "false" = Production: Strict TLS validation, generic error messages for security
DEVELOPER_MODE=false

# Authentication mode
# MSSQL_AUTH: "sql" (default) = SQL Server authentication using MSSQL_USER/MSSQL_PASSWORD
# MSSQL_AUTH: "integrated" or "windows" = Windows Integrated Authentication (SSPI). Only works on Windows, requires the process to run under a domain or user with DB permissions.
# MSSQL_AUTH: "azure" = Azure AD authentication (not implemented by default, additional setup required)
# Example:
# MSSQL_AUTH=sql

# Example configurations for different environments:

# === Azure SQL Database ===
# MSSQL_SERVER=your-server.database.windows.net
# MSSQL_DATABASE=YourAzureDB
# MSSQL_USER=your_user@your-server
# MSSQL_PASSWORD=your_azure_password
# MSSQL_PORT=1433
# DEVELOPER_MODE=false

# === On-premises SQL Server (Production) ===
# MSSQL_SERVER=sql-prod.company.local
# MSSQL_DATABASE=ProductionDB
# MSSQL_USER=prod_user
# MSSQL_PASSWORD=very_secure_production_password
# MSSQL_PORT=1433
# DEVELOPER_MODE=false

# === Local Development Server ===
# MSSQL_SERVER=localhost
# MSSQL_DATABASE=DevDB
# MSSQL_USER=dev_user
# MSSQL_PASSWORD=dev_password
# MSSQL_PORT=1433
# DEVELOPER_MODE=true

# === Docker SQL Server ===
# MSSQL_SERVER=127.0.0.1
# MSSQL_DATABASE=TestDB
# MSSQL_USER=sa
# MSSQL_PASSWORD=YourStrong@Passw0rd
# MSSQL_PORT=1433
# DEVELOPER_MODE=true

# =============================================================================
# SECURITY & ACCESS CONTROL
# =============================================================================

# Read-only mode: Blocks INSERT, UPDATE, DELETE, DROP, etc.
# "true"  = Only SELECT queries allowed (recommended for AI/Claude access)
# "false" = Full access (use with caution)
# MSSQL_READ_ONLY=true

# Table whitelist for modifications (only applies when MSSQL_READ_ONLY=true)
# Comma-separated list of tables that CAN be modified even in read-only mode
# Useful for temporary/staging tables that AI can use
# MSSQL_WHITELIST_TABLES=temp_ai_work,staging_data,v_temp_imports

# Stored procedure whitelist for execute_procedure tool
# Comma-separated list of procedures that can be executed
# If empty, execute_procedure tool is disabled
# MSSQL_WHITELIST_PROCEDURES=sp_GetCustomerOrders,sp_GenerateReport,usp_SearchProducts

# Maximum query size in characters (default: 1MB = 1048576)
# MSSQL_MAX_QUERY_SIZE=1048576