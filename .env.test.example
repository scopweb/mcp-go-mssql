# Test Environment Configuration
# Copy this file to .env.test and fill in with your TEST database credentials
#
# IMPORTANT SECURITY NOTES:
# - NEVER commit .env.test to version control
# - Use a SEPARATE test database, NOT production
# - Use test/throwaway credentials only
# - Delete .env.test before committing changes
#
# This file should be in .gitignore

# Required: Test SQL Server hostname or IP address
MSSQL_SERVER=test-server.local

# Required: Test database name (should be a dedicated test DB)
MSSQL_DATABASE=TestDB

# Required: Test username (should be a test user with limited permissions)
MSSQL_USER=test_user

# Required: Test password (use a test-only password)
MSSQL_PASSWORD=test_password_only

# Optional: SQL Server port (defaults to 1433)
MSSQL_PORT=1433

# Development mode for testing (true allows self-signed certificates)
DEVELOPER_MODE=true

# Authentication mode
MSSQL_AUTH=sql

# Read-only mode disabled for testing (tests need write access)
MSSQL_READ_ONLY=false

# Optional: Whitelist tables for testing (empty by default)
MSSQL_WHITELIST_TABLES=
